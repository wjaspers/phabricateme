(function(window) {
	'use strict';
	var document = window.document, ph = window.PhabricateMe;
	
	document.addEventListener('DOMContentLoaded', function () {
		var authorization, shortcuts;
		authorization = ph.fetchPlugin('Authorization');
	});

})(window);
